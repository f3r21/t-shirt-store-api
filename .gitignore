# Build output and installed packages
/dist
/node_modules
/coverage

# Local environment
.env

# Written by `prisma generate`
/src/generated/

# The style packages `vale sync` downloads; the config, the project vocabulary
# and the project rules beside them are committed
/.vale/styles/Google/
/.vale/styles/Readability/

*.log
.DS_Store
