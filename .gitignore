# Build output and installed packages
/dist
/node_modules
/coverage

# Local environment
.env

# Written by `prisma generate`
/src/generated/

# The style package `vale sync` downloads; the config and the project vocabulary
# beside it are committed
/.vale/styles/Google/

*.log
.DS_Store
